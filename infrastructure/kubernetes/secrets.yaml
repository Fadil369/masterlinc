# WARNING: This file contains placeholder secrets for demonstration only.
# NEVER commit actual secrets to version control!
# 
# Before deploying to production:
# 1. Generate strong random secrets for all values
# 2. Use a secrets management solution (HashiCorp Vault, AWS Secrets Manager, etc.)
# 3. Enable encryption at rest for Kubernetes secrets
# 4. Rotate credentials regularly
# 5. Implement RBAC to limit access to secrets
#
# Example of generating strong secrets:
# openssl rand -base64 32  # For JWT_SECRET and ENCRYPTION_KEY
# openssl rand -hex 16     # For database passwords
#
apiVersion: v1
kind: Secret
metadata:
  name: masterlinc-secrets
  namespace: masterlinc
type: Opaque
stringData:
  POSTGRES_USER: "masterlinc"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
  ENCRYPTION_KEY: "CHANGE_ME_IN_PRODUCTION"
  OPENAI_API_KEY: "CHANGE_ME_IN_PRODUCTION"
  NPHIES_LICENSE_KEY: "CHANGE_ME_IN_PRODUCTION"
---
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secrets
  namespace: masterlinc
type: Opaque
stringData:
  GRAFANA_ADMIN_USER: "admin"
  GRAFANA_ADMIN_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
